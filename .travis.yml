language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
script:
  - python app/manage.py test
before_install:
  - openssl aes-256-cbc -K $encrypted_f8911ab1b4a2_key -iv $encrypted_f8911ab1b4a2_iv -in .env.enc -out .env -d
  - tar xvf secrets.tar
